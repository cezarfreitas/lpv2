version: "3.8"

services:
  ecko-landing:
    build:
      context: .
      dockerfile: Dockerfile.safe
      args:
        VITE_api_form: https://470187c48f0a4640803d23a0491ae11b-a421d35e00a9431bb90c3d034.fly.dev/api/leads
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - PORT=80
    restart: unless-stopped

  # Para desenvolvimento - descomente se necessário
  # ecko-landing-dev:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #     target: builder
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - NODE_ENV=development
  #     - VITE_api_form=https://470187c48f0a4640803d23a0491ae11b-a421d35e00a9431bb90c3d034.fly.dev/api/leads
  #   volumes:
  #     - .:/app
  #     - /app/node_modules
  #   command: npm run dev
